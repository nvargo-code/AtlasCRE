# Database (Neon PostgreSQL)
DATABASE_URL="postgresql://user:password@host.neon.tech/atlas_cre?sslmode=require"

# NextAuth
NEXTAUTH_SECRET="generate-a-random-secret-here"
NEXTAUTH_URL="http://localhost:3000"

# Optional: Mapbox token (if switching from MapLibre default tiles)
# NEXT_PUBLIC_MAPBOX_TOKEN=""
